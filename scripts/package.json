{
  "name": "scripts",
  "version": "1.0.0",
  "description": "Local Cortex content, controller and browser verification tools",
  "scripts": {
    "test": "cd .. && node --test --test-concurrency=4 scripts/test-*.cjs"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "playwright": "^1.61.0"
  },
  "private": true,
  "devDependencies": {
    "jsdom": "30.0.1"
  }
}
