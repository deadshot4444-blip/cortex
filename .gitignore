# Regenerable generation artifacts (large, not needed to run or deploy)
data/raw/

# OS / editor junk
.DS_Store
*.swp

# Local agent/editor config (machine-specific, e.g. preview launch.json)
.claude/

# Local build outputs
node_modules/
*.zip
